export interface RemediationAction { action: string; resource?: string; file?: string; path?: string; variable?: string; package?: string; before?: unknown; after?: unknown; value?: unknown; }
export interface Remediation { id: string; changeId: string; resourceId: string; affectedResourceIds: string[]; blastRadius: { directCount: number; transitiveCount: number; totalCount: number; maxDependencyDepth: number }; securityFindingIds: string[]; type: string; title: string; description: string; reason: string; proposedChanges: RemediationAction[]; validation?: { schemaValidated: boolean; valid: boolean; checks: Array<{ name: string; passed: boolean; message: string }>; validatedAt: string }; risk: { score: number; level: string; requiresApproval: boolean }; status: string; approval?: { approved: boolean; approvedBy: string; approvedAt: string; reason: string }; execution?: { mode: 'dry_run'; success: boolean; message: string }; verification?: { status: string; summary: string; evidenceIds: string[] }; rollback: { available: boolean; actions: Array<{ action: string; path: string; value: unknown }>; reason?: string }; evidenceIds: string[]; createdBy: string; prStatus: string; prProposal?: Record<string, unknown>; audit: Array<{ event: string; status: string; actor: string; at: string }>; }