import { Router } from 'express';
import { analyzeRemediationController, approveRemediationController, dryRunRemediationController, generateRemediationController, getRemediationController, listRemediation, patchRemediation, postRemediation, preparePRController, rejectRemediationController, validateRemediationController, verifyRemediationController } from './remediation.controller.js';
import { authorize } from '../middleware/auth.middleware.js';
export const remediationRouter = Router();
remediationRouter.post('/', postRemediation); remediationRouter.get('/', listRemediation); remediationRouter.get('/:id', getRemediationController); remediationRouter.patch('/:id', patchRemediation); remediationRouter.post('/:id/analyze', analyzeRemediationController); remediationRouter.post('/:id/generate', generateRemediationController); remediationRouter.post('/:id/validate', validateRemediationController); remediationRouter.post('/:id/prepare-pr', preparePRController); remediationRouter.post('/:id/approve', authorize('admin'), approveRemediationController); remediationRouter.post('/:id/reject', authorize('admin'), rejectRemediationController); remediationRouter.post('/:id/dry-run', dryRunRemediationController); remediationRouter.post('/:id/verify', verifyRemediationController);